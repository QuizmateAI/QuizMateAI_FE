/**
 * Display labels for AI quiz output language (sent as plain string to the API).
 * Kept in sync with individual workspace AI quiz defaults where applicable.
 */
export const AI_OUTPUT_LANGUAGES = ["Vietnamese", "English"];
